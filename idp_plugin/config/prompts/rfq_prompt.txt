Extract structured data from this Request for Quotation (RFQ) document.

CRITICAL RULES:
- Extract ONLY values explicitly stated in the document
- Use null for any missing values (NOT empty string, NOT "N/A", NOT assumptions)
- Do NOT infer or guess any information
- Preserve exact text as it appears in the document
- For dates, extract in YYYY-MM-DD format if possible, otherwise preserve as-is
- For arrays (products, testing requirements), include all items found or empty array if none

Fields to extract:
1. RFQ number/reference (if present)
2. RFQ date
3. Customer information (name, address, contact person, email, phone)
4. Products/services requested (product name, description, quantity, specifications)
5. Testing requirements (test name, standard, method, quantity)
6. Delivery requirements (delivery date, location, special instructions)
7. Commercial terms (currency, payment terms, validity period)
8. General remarks

Return the extracted data as JSON matching the provided schema. Use null for any field that is not explicitly found in the document.





