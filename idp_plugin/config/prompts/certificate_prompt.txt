Extract structured data from this Test Certificate document.

CRITICAL RULES:
- Extract ONLY values explicitly stated in the document
- Use null for any missing values (NOT empty string, NOT "N/A", NOT assumptions)
- Do NOT infer or guess any information
- Preserve exact text as it appears in the document
- For dates, extract in YYYY-MM-DD format if possible, otherwise preserve as-is
- For arrays (samples, test results, signatories), include all items found or empty array if none

Fields to extract:
1. Certificate number/reference
2. Issue date and validity date
3. Customer information (name, address)
4. Product information (name, description, model, batch number, manufacturing date)
5. Samples tested (sample number, description, received date, tested date)
6. Test results (test name, standard, method, result, unit, specification, status, associated sample number)
7. Laboratory information (name, accreditation, address)
8. Signatories (name, designation, signature date)
9. General remarks

Return the extracted data as JSON matching the provided schema. Use null for any field that is not explicitly found in the document.





