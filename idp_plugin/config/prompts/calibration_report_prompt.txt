Extract structured data from this Calibration Report document.

CRITICAL RULES:
- Extract ONLY values explicitly stated in the document
- Use null for any missing values (NOT empty string, NOT "N/A", NOT assumptions)
- Do NOT infer or guess any information
- Preserve exact text as it appears in the document
- For dates, extract in YYYY-MM-DD format if possible, otherwise preserve as-is
- For arrays (calibration standards, calibration results), include all items found or empty array if none

Fields to extract:
1. Report number/reference
2. Report date, calibration date, next calibration date
3. Customer information (name, address, contact person)
4. Equipment information (name, manufacturer, model, serial number, asset number)
5. Calibration standards used (standard name, certificate number, validity date)
6. Calibration results (parameter, nominal value, measured value, uncertainty, unit, status, specification)
7. Environmental conditions (temperature, humidity, pressure)
8. Laboratory information (name, accreditation, address)
9. Calibrated by and approved by names
10. General remarks

Return the extracted data as JSON matching the provided schema. Use null for any field that is not explicitly found in the document.





